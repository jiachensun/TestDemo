<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachingdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".room.JetPackRoomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myaop.TestAopActivity"/>
        <activity android:name=".eventbus.MyEventBus3Activity" />
        <activity android:name=".livedata.LiveData2Activity" />
        <activity android:name=".livedata.LiveData1Activity" />
        <activity android:name=".eventbus.MyEventBus2Activity" />
        <activity android:name=".eventbus.MyEventBus1Activity" />
        <activity android:name=".jetpackmvp.view.JPMvpActivity" />
        <activity android:name=".hotfix.HotfixActivity" />
        <activity android:name=".recyclerview.RecelviewActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.SplashActivity"
            android:description="@string/string_activity_description_splash"
            android:theme="@style/SplashActivityTheme" />
        <activity
            android:name=".activity.MainActivity"
            android:description="@string/string_activity_description_main">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>